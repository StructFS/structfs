[package]
name = "structfs-json-store"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "JSON-based store implementations for StructFS"

[dependencies]
# Legacy (to be deprecated)
structfs-store = { workspace = true }

# New architecture
structfs-core-store = { path = "../core-store" }
structfs-serde-store = { path = "../serde-store" }

serde = { workspace = true }
serde_json = { workspace = true }
erased-serde = { workspace = true }
lazy_static = { workspace = true }
regex = { workspace = true }
log = { workspace = true }
walkdir = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
structfs-store = { workspace = true, features = ["test-utils"] }
